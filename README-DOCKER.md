# Docker Deployment Guide

## Prerequisites
- Docker (v20.10+)
- Docker Compose (v2.0+)

## Quick Start

### 1. Environment Setup

Copy the example environment file and configure your API keys:

```bash
cp .env.example .env