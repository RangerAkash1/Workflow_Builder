replicaCount: 1

elasticsearchHosts: "http://elasticsearch-master:9200"

service:
  type: ClusterIP
  port: 5601

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
